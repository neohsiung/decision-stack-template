# 貢獻指南

## 這個 repo 是什麼

一套讓你建立**自己**的判斷原則庫的框架：三層分層規格、寫入程序、檢索工具。

它不是一份判斷原則的集合。`Models/`、`Frameworks/`、`Decisions/` 在模板裡是空的，那是設計。

這個目的決定了下面收什麼、不收什麼。

## 收什麼

| 路徑 | 收 |
|---|---|
| `tools/**` | 工具的功能、修正、效能 |
| `README.md` | 規格的釐清、矛盾、寫得不清楚的地方 |
| `templates/**` | 各層骨架 |
| `examples/**` | 去識別化的示範條目，明確標教學用 |
| `.github/**` | CI、issue 與 PR 範本 |
| `tools/local.example/**` | 本機區的範本 |

## 不收什麼

`Models/`、`Frameworks/`、`Decisions/`、`MEMORY.md` 的條目。

兩層理由，第一層是硬的。

**第一層：所有權。** 條目的「事件」節寫的是一件真實發生過的事。那件事的素材屬於它發生的那個
組織，不屬於寫下它的人。判準是兩題，不是「隱去名字後還認得出來嗎」：

- 這東西是誰的資產？
- 外部人本來就看得到嗎？

辨識度沒有終點。每多隱去一層都還可以再問一次像不像，而寫的人永遠比讀的人知道得多，會系統性
高估辨識度。所以它同時導致做過頭與做不夠。所有權判得快，而且判得完。

**第二層：訊號。** `origin` 欄位記的是什麼揭露了這條原則。它唯一的價值是這行統計：

```bash
grep -h '^origin:' */*.md | sort | uniq -c
```

「這套流程實際產出了什麼」只有這個欄位答得出來。收進別人的條目，這個數字就不再是任何人的
判斷紀錄。

條目請留在你自己的 repo。那才是它們會被檢索到、會被用上的地方。

## `examples/` 的收錄門檻

示範條目是例外，但門檻是機械的。三題全部要答「是」：

- `origin` 是 `external_reference` 或 `seeded` 嗎？
- 沒有第一人稱的事件段嗎？
- 拿掉所有組織、專案、人名之後，內容一個字都不用改嗎？

有 `judgment_case` 事件段的一律不收，不管去識別化做得多乾淨。

## 去識別化條款

這個 repo 的主題是記錄你的判斷被打臉的過程，貢獻內容因此天然容易夾帶你所在組織的資訊。

送出 PR 前逐項檢查，兩組問題都要過：

**第一組問名字。** 無同事姓名含英文暱稱、無專案代號、無內部系統名稱、無營收與人力數字。

**第二組問時空：換一台機器、換一個人，這句話還成立嗎？** 這一組擋的是沒有名字、但只對一個人
一台機器成立的句子。日期、PR 號、版本號、「這台」「當時」「上次」都是徵狀。

`tools/stack lint --shippable` 機械檢查第二組。pre-commit 會跑它。

第三方可識別資訊不得出現在任何貢獻內容裡，包含 commit message 與 PR 描述。

## PR 的範圍

**一個 PR 一個連貫的主題，以最小的完整範圍為單位。** 判準是合併之後系統自洽：只加工具不改
規格等於沒生效，只改規格不加工具是壞的，那兩件事屬於同一個 PR。一條新規格跟一個工具修正
沒有這種相依，就是兩個 PR。

**完整範圍有一個上限：一次 review 讀得完。** 審不完的 diff 等於沒有被審。

## 出處聲明

commit 加 `Signed-off-by:`，表示你同意
[Developer Certificate of Origin](https://developercertificate.org/)：你有權提交這份內容，
而且它可以用本專案的授權散布。

```bash
git commit -s -m "<訊息>"
```

不用簽 CLA。CLA 的門檻會擋掉真正想改一行工具的人。

## 送出前跑三件事

```bash
stack lint              # 索引、連結、frontmatter、upstream、詞彙表
stack lint --shippable  # 可升級區有沒有夾帶只對本機或本人成立的文字
stack doctor --self-test
```

改動檢索相關的東西時多跑 `stack eval`，對照 `--save-baseline` 存的基線。命中只能持平或變好。

## 寫法

貢獻的文字照 WRITING §寫作指南。重點三條：一句一個意思；概念用本名不用比喻；還沒驗證的判斷
寫成還沒驗證。
