# 召回率回歸語料。一行一題：<實際問過的問題>TAB<應該命中的條目，逗號分隔>
#
# 這是範例檔，裡面沒有任何一題。`stack eval` 讀的是 tools/regression.tsv，它不進版控，
# 因為題目是你自己的。複製一份再開始寫：
#
#     cp tools/regression.tsv.example tools/regression.tsv
#
# 答案那一欄只能指向被索引的條目，也就是 Models/、Decisions/、Frameworks/、Expressions/
# 底下的檔案。examples/ 不在索引範圍內，指過去會固定拿到 rank 999。
#
# 格式長這樣（照抄會失敗，因為這兩個路徑是虛構的）：
#
#     口頭報一個數字之後那個數字會怎麼被記住<TAB>Decisions/your-entry.md
#     估一件沒做過的事第一步做什麼<TAB>Frameworks/your-framework.md,Decisions/another.md
#
# 這份語料是「工具有沒有偷偷變差」唯一的證據。每次改嵌入取材、換模型、大批拆分條目之後都要
# 重跑，命中只能持平或變好。
#
# 四條寫法規則：
#
# 1. 題目一律用**當初真的問過的原話**。為了讓分數好看而改寫成貼近條目用詞的句子，會讓這份
#    語料變成自我印證。
# 2. 新增題目的時機是真實對話裡出現一個「該撈到某筆但沒撈到」的案例。那一題比十題自己想的
#    都值錢。
# 3. 允許一題標多個答案，但只有在那幾筆**真的都回答了這一題**時才可以加。因為分數不好看而
#    追加一個剛好出現在結果裡的條目，跟改寫題目一樣是自我印證。
# 4. 判準看的是命中集合，不是單一筆的名次。條目變細之後某一筆的排名下滑，可能是變精準的
#    結果而不是退步。這種情況把原因寫成一行註解留在該題旁邊。
#
# 已知盲點（不是失敗，是索引範圍的邊界）：SPEC 的分層判準、tools/context/ 的使用契約與寫入
# 程序這類「後設判準」不在索引裡。問「這個想法該放哪一層」時撈不到東西是正常的。
