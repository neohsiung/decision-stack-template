# 寫作指南

寫一筆條目之前讀一次。

分層判準與寫入程序看 [SPEC.md](SPEC.md)，可以照著看的完整實例在 [examples/](examples/)。

每一筆條目寫給不帶脈絡的下一個讀者。讀者手上沒有寫入當下的對話、票卡、程式碼。AI 和三個月後
的你是同一種讀者。自問：讀者從哪裡知道這個指涉？答案是「從寫的人腦裡」，就補一句或刪掉。

## 誰負責哪一條

| 規則 | 誰負責 |
|---|---|
| 分節存在與順序、首句存在、首句不含 `——`／`；`／`：`、`related:` 目標存在、正文沒有「相關：」段、受控詞彙、`upstream:` 方向 | `lint` 擋，寫錯就 commit 不進去 |
| 句中破折號密度、句均字數、`不是X是Y`、`前者／後者`、`靜默／亮紅燈` | `lint` 提醒，讀過再決定 |
| 這份文件其餘所有規則 | 只能你看 |

**前兩欄不用背。** 跑 `stack lint`，它會講。底下的篇幅都給第三欄。

## 命名

檔名是英文 kebab-case 的主張句，例如 `grouping-key-is-verified-not-assumed`。主題標籤如
`grouping-key` 不合格。在索引與 `recall` 輸出裡，檔名是讀者最先看到的一句話。

用英文是為了連結、錨點與工具穩定。內文用中文。

名字指向內容重心。重心用行數量：哪一段寫最多，名字就講那一段。條目被叫出來的場合不是重心。

## 正文結構

每層的分節固定，順序固定。某一節暫時沒內容時保留標題並寫「待補」。

| 層 | 分節 | 這一層回答的問題 |
|---|---|---|
| Model | 信念 → 適用界線 → 不適用的情況 | 為什麼這樣想 |
| Framework | 用在什麼時候 → 步驟或檢查面 → 停止條件 | 怎麼做 |
| Decision | 規則 → 事件 → 為什麼 → 怎麼用 | 做什麼，以及證據 |
| Expression | 適用的格子 → 規格 → 骨架 → 不用它的情況 | 寫成什麼樣 |

第一節第一句是粗體規則句，只講做什麼。為什麼與怎麼做各自成節。

`lint` 擋接續記號。**一個子句這件事它查不到，靠你。**

**Framework 的第一節叫「用在什麼時候」，但第一句仍然只講做什麼。** 觸發情境寫在那一句底下。
把「什麼時候用」寫成首句，那一句就變成查詢的形狀，會在不相關的查詢裡蓋過別的條目。改完一批
之後跑 `stack eval` 對照基線就看得到。

Decision 的「事件」節不可省，它是這條規則的證據。寫到讀者看得出「原本以為什麼、實際是什麼」
就夠。

## 對照其他條目

族譜走 frontmatter `upstream:`。近似但不等價的對照走 `related:`。內文不寫「相關：」段。

`related:` 一筆一行。`—` 後面接一句分界句，說清楚那筆答什麼、本筆答什麼：

```
related: <條目檔名> — 那筆答〈一句〉；本筆答〈一句〉
```

分號不是要件，句號或逗號分界一樣算數。**要件是分界句本身存在。**

用檔名，不用「那條」「本條」「前者」「後者」。`recall` 的一跳鄰居沿 `related:` 展開，所以
對照搬出正文之後不會從檢索裡消失。

## 句子

寫完掃五樣，有就刪：

1. 破折號接補述。「問一句——自動化之後…」改成「判斷標準是…」。
2. 對仗式勸慰收尾。「不要卡在那裡」「不用自己先判到底」這類。
3. 括號補充說明。該講的寫進正文，不該講的刪掉。
4. 標語式宣告。「答得出來就算數」「只有一件事明確不算」這類。
5. 情緒鋪陳。「一時想不出來是正常的」這類。

另外四條：

- 一句一意。長句拆短。刪字之後讀者看不出「檢查什麼、誰對誰、到什麼程度」時，寧可多幾個字。
- 每句分清是為什麼、做什麼、還是怎麼做。混在一句就拆開。
- 不用自造的比喻。「靜默」寫成「不產生訊號」，「亮紅燈」寫成「檢查失敗」。判斷法：換成字面
  直述後資訊沒少，原詞就是裝飾。
- 回指要具名。跨段落或跨檔案指涉時寫出名稱。讀者要往回翻才對得上的代詞，換成名字。

**「形狀」「漏」不在比喻那一條裡。** 兩者都量過。

「漏」60 次，全是中文常用義，像「漏了驗證階段」「必漏上限」。「形狀」65 次，已經是全庫採用
的術語，跟「閘門」「骨架」同類。列進禁用詞會擋掉正確的寫法。

## 中文語序

英文句法會留下殘影，讀起來像翻譯稿。四個常見來源：

- **不定冠詞。** 「一個」直譯自 a 或 an。中文不用量詞也完整：「一個人出現在對話的周邊」
  寫成「人出現在對話裡」。
- **抽象名詞當主語。** 「相鄰性不是作者身分」「猜錯的代價不對稱」。中文習慣人或動作當主語：
  「人在旁邊，不等於話是他說的」「這兩種錯法的代價差很多」。抽象句可以留著當標籤，
  後面接一句用人當主語的話把它說清楚。
- **造詞式角色名。** 「被驗證者」「驗證者」是為了對稱而造的詞。改回「對方」「自己」
  「被查的那一方」。
- **句中破折號。** 拿破折號當連接詞接第二個意思，是 em dash 的用法。改成句號、分號或冒號。
  條列項的標籤分隔不在此列，`- **成本不對稱**——從基底重算` 是版面不是句法。

還沒驗證的判斷寫成還沒驗證。「目前傾向」「還沒驗過」「只試過一次」都可以。寫成完成態會讓
下一個讀者把猜測當成事實。

`lint` 的兩個密度門檻只是警訊。句中破折號每千字 2.5、句子平均 55 字，超過就提醒。兩個數字都從
實際改寫過的條目量出來，不是訂出來的。引號密度刻意不設門檻，改寫過的條目裡引號最密的那一筆
也是最好讀的一筆。

## frontmatter

```yaml
---
name: <與檔名同的 kebab-case>
description: <一行摘要，決定檢索時的相關性判斷>
type: model | decision | framework | expression   # 與所在資料夾一致
origin: seeded | judgment_case | code_review | self_caught | elicited | external_reference | repo_sync
recorded: <入棧日期，YYYY-MM-DD>
confirmed: <選用，被獨立再次印證的日期，逗號分隔可多筆>
upstream: <選用，上游條目的 repo 相對路徑，逗號分隔可多筆；方向嚴格向上：decision → Frameworks/ 或 Models/，framework → Models/，model 不設；expression 選用，填了要指進三層>
audience_direction: <僅 expression，必填。對上 | 水平 | 對下>
audience_role: <僅 expression，必填。決定者 | 執行者 | 被諮詢者 | 被知會者>
channel: <僅 expression，必填。即時訊息 | 電子郵件 | 票卡留言 | 溝通文件 | 簡報>
related: <選用，近似但不等價的對照，一筆一行，格式見 §對照其他條目>
downstream: <選用，由這筆衍生的下游產物，可多筆>
---
```

`recorded` 寫入棧當天，不回填。批次進來的條目日期會集中在同一天，那是事實。編造一個看起來
合理的分佈，會毀掉這個欄位唯一承載的訊號：這條原則當初是怎麼進來的。

由既有條目拆出來的新檔，`origin` 與 `recorded` 沿用母條目。拆分不是新內容進棧。拆檔日寫成
`recorded` 會讓這個欄位變成「什麼時候整理過」，那個訊號沒有人需要。

`confirmed` 只在某條原則被另一個獨立事件再次印證時追加。這是這裡唯一的成熟度訊號。不用寫
入當下的自評分數。

不排定期重新驗證。可攜原則不像票況會過期。真正的失效有兩種：從單一事件過度一般化，以及同一
條原則在兩個檔裡各自演化。兩種都靠寫入前的 dedupe 與週期性的 curation 抓。

淘汰問的是另一題：這筆有沒有被任何檢索情境需要過。指標見 SPEC §各層契約·淘汰。

`downstream` 列出由這筆衍生的下游產物，一行一筆。受 SPEC §去識別化判準約束：個人工具可以寫
名字，組織側產物只寫通用描述。例如寫「職級階梯文件」，不寫實際文件標題。它存在的理由是傳播，
上游變精準時下游不會自動跟上。
