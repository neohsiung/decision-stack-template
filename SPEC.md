# 規格

決策棧的操作規格。**查閱用，不必從頭讀到尾**——遇到「這該放哪一層」「這句話能不能公開」
「寫入要走哪幾步」時翻到對應的一節。

入門與全貌看 [README.md](README.md)，寫條目的文字規則看 [WRITING.md](WRITING.md)。

## 歸檔指南

新內容進 repo 之前先問一題：**拿掉組織的專案名、人名、系統名之後，這個判斷還站得住嗎？**

站不住的留在組織範圍的記憶裡。站得住的進來，依 §內容分層 決定放哪一層。

需要新分類時才建資料夾。不預先建空的。

## 去識別化判準

寫入前逐項檢查，兩組問題，兩組都要過。

**第一組問名字。** 內容不含具體專案代號、同事姓名、內部系統名稱、營收與人力數字。
「某次在 X 系統上因為 Y 專案發生的事」寫成「當 <情境類型> 發生時」。保留判斷邏輯，去掉可回溯到
特定組織的識別資訊。

**第二組問時空：換一台機器、換一個人，這句話還成立嗎？** 成立的可以留在會被複製給別人的區域。
不成立的有三個去處。本機狀態進 `tools/local/`。個人事故的證據段留在私有 repo 的條目裡。兩者都
不是的刪掉。`stack lint --shippable` 機械檢查這一組，pre-commit 對每次 commit 跑它。

第一組擋名字。第二組擋「這台機器的 gh 有公司帳號」這類沒有名字、但只對一個人一台機器成立的
句子。只做第一組不會對第二類產生任何訊號。

## 內容分層

這個 repo 存的是上游的思維，不是思維的下游產物。混在一起會讓「換了組織還能帶走什麼」這個
問題答不出來。製作某份文件的說明換了組織未必還適用。判斷的軸永遠適用。

| 層 | 位置 | 放什麼 | 判準 |
|---|---|---|---|
| 核心信念 | `Models/` | 少數不變的核心信念，預期 5 到 15 筆。一句信念句加上它的適用界線。回答「為什麼這樣思考」。例：訊號不是狀態 | 它解釋判斷的動機，不是方法。換掉所有情境與步驟仍然成立。被多副骨架共同依據 |
| 思考骨架 | `Frameworks/` | 思考與作業的骨架。回答「怎麼下判斷」。收兩類：作業骨架（格式、結構、有序步驟、並列判準）與思考技法（有步驟、檢查面或多軸展開的思考方法，例：五個為什麼） | 內容是結構或方法。不綁單一產物 |
| 事件原則 | `Decisions/` | 由具體事件或糾正揭露的判斷原則。最下游 | 說得出一個觸發它的事件，以及它依據的基準：Model 或 Framework |
| 產出用語 | `Expressions/` | 一格產出該用什麼詞、什麼語氣、放哪些內容，照作者本人的講法。加上可複製的骨架。回答「這次要寫成什麼樣」 | 對象與渠道兩個維度落得下來。它是推導鏈末端產物的形狀，不是鏈上的第四環 |
| 承載層 | `tools/` 加 `skills/` | 讓這份內容可被檢索、可被維護的最小工具集與程序 | 換到任何 harness 或服務都需要它才能用這份內容 |
| 下游產物 | 棧外 | 綁組織識別的 skill、指南、制度設計、表單設計 | 由上游層的 `downstream:` 指向。不進這個 repo |

下游產物留在棧外。它們為特定組織、特定工具、特定時點而做，會過期，而且多半含有組織識別資訊。
棧裡只留一條指向它的線，讓「上游變精準時該回頭看哪些東西」查得到。

下游產物與 `Expressions/` 的分界一句：**被執行的那份模板留在棧外，那一格的形狀規格進棧。**
一份週報模板住在跑週報的那個流程裡，它才是被執行的那一份；把它複製進棧會多出一份各自演化的
副本，而贏的永遠是被執行的那一份。棧裡那筆寫的是「這一格的產出為什麼長這樣」與「規格是什麼」，
再用 `downstream:` 指向被執行的正本。棧外沒有正本的格子才把骨架全文寫進來。

承載層與下游產物的分界一句：它服務的是這份內容本身，還是某個特定組織的某個流程？服務內容本身
的跟著 repo 走，服務組織流程的留在棧外。四個例子：

- `tools/stack` 服務內容本身。把它推出棧外，這份內容搬到下一個服務時就只剩一堆 markdown，沒有檢索
  能力。檢索能力是這些內容會被用到的原因。
- `tools/regression.tsv` 同理。它是「檢索有沒有變差」唯一的證據，跟工具一起走才有意義。
- `tools/context/` 同理。規則的正本要住在有版控與審查的地方，各 harness 的載入通道只放指標。
  把片段推出棧外，生效的會變成某個設定檔裡的手抄副本。
- `skills/` 放服務這份內容本身的程序，例如覆盤整理的 curation skill。它們跟 `tools/stack` 同一層。
  內容搬走時沒帶著程序，維護方法就斷了。每個 skill 開頭寫明使用時機。

組織情境的 skill 是下游產物，留在棧外。skill 也走同一套收斂：建立前 dedupe，一個 skill 一個目的，
可增可減。方法的數量不是資產，被執行的方法才是。

跨層的檔案不硬切。一份東西前半是判斷、後半是製作說明時，留在較上游的那一層，把製作說明那半
用 `downstream:` 標出去。等它下次真的被引用時再決定要不要拆檔。

### 各層契約：目標、權責、異動條件

越上層越抽象、越穩定。`Models` 是少數不變的核心信念：為什麼這樣思考，換掉所有情境與步驟仍
不變的理由。`Frameworks` 是思考的骨架與技法：由信念長出的格式、結構、程序與檢查面，回答怎麼
下判斷。`Decisions` 是最下游的產物：骨架或信念在單一事件上套用的具體結果。內容沿這條軸雙向
流動：下層累積出上層，叫升級；上層修正後回頭校正下層，叫傳播。這一節是層級定義與升降級判準
的正本，其他段落只引用不重述。

Model 與 Framework 的分界一句話：信念解釋動機，骨架給出方法。一筆 Model 寫著寫著長出檢查面、
步驟清單或多欄展開，就是它其實是 Framework、或該拆成一句信念加一副骨架的訊號。「一句話說得完」
仍是 Model 的必要條件，但不充分。一句話說得完的方法，例如技法口訣，也是 Framework。Models 的
預期規模是 5 到 15 筆。超過時優先懷疑混進了技法，不放寬判準。

決策必有基準。一筆 Decision 的判斷依據來自某個 Model 或某個 Framework。說不出基準的決策沒有
可檢驗的依據。所以 Decision 的 `upstream:` 對新條目是必備，這是寫入程序 step 3 的基準閘門，見
[`tools/context/write-procedure.md`](tools/context/write-procedure.md)。答不出基準就不建檔。
互動中攤開候選跟使用者討論，非互動暫存 `Pending/`。不為單筆硬造 1:1 復述的上游。討論的合法
出口包含使用者明示裁決留孤兒，等同構的第二筆出現時再抽。閘門生效前留下的孤兒在 `stack lint`
覆蓋率缺口清單裡，是 curation 的輸入。

骨架必有信念，與上一條對稱，同一道閘門。新 Framework 要說得出它由哪個信念長出來。說不出就走
同一條討論或暫存路徑。同樣不為單筆硬造 1:1 的上游。三層因此構成一條完整的推導鏈：信念（為什麼
這樣思考）→ 骨架（怎麼下判斷）→ 決策（下了什麼判斷）。

`Expressions/` 不在這條鏈上。鏈回答的是怎麼判斷，它回答的是判斷完之後那份東西要寫成什麼樣。
所以它是目錄不是第四層，走的閘門也不同：三層的閘門是「說得出上游」，它的閘門是**軸必填**。
`upstream:` 對它是選用的出處標註，填了就要指進三層。它不進 `stack tree` 的格狀結構，也不進
upstream 覆蓋率的缺口報告。

**軸是組織鍵，不是檢索鍵。** 它管三件事：`stack lint` 查值在受控詞彙裡、索引按它分組、
矩陣攤開時看得出哪一格還沒有人填。檢索走 `description`，所以那一行要寫出這一格的情境。
試過把軸合成一句餵進向量索引，結果權責那幾個詞撞上責任歸屬的查詢，把一筆不相干的條目
擠出 top-3，而拿掉之後兩筆 Expression 在自己的動作查詢上照樣是第一名。

### Expressions 的軸

**兩個維度：對象、渠道。** 對象由方向與權責兩件事決定，所以 frontmatter 是三個鍵。

| 鍵 | 它回答哪一題 | 值 |
|---|---|---|
| `audience_direction` | 對方跟你的相對位置。**它決定資訊落差**：對方已經知道什麼、哪些術語要展開、哪些背景可以省 | 對上、水平、對下 |
| `audience_role` | 對方在這件事上的位置。**它決定敬語與句式**：你要對方付出什麼 | 決定者、執行者、被諮詢者、被知會者、轉傳者 |
| `channel` | 讀者以什麼方式接觸到這份東西。不是它用哪套軟體送出去的 | 見下表 |

方向與權責的分工要記牢，兩者常被混成一件事。客氣的份量跟著「你要對方付出什麼」走，不跟著
職級走，那是權責這一軸管的；方向管的是他手上已經有什麼資訊。

**轉傳者是被知會者的延伸，分界在他是不是終點站。** 被知會者讀完就過去了，轉傳者讀完要把
同一件事再講一次給他的下游，所以他需要的比終點站多兩樣：授權來源，以及他自己那一節要做
什麼。它跟執行者也不同——執行者要完成一件任務，轉傳者要完成的是把訊息送到下一節。有層級的
組織裡佈達鏈是常態，這一格獨立出來，`related:` 才有辦法把兩種公告的分界寫清楚。

`audience_role` 借責任分配矩陣的詞當名字是因為那組詞最好懂，收的卻不是完整的權責角色，
是收件人對這件事的五種立場。真的要指派權責時另有一副骨架，那一副明說那套矩陣不是唯一選項，
依這次要解的問題挑。角色是情境的函數，所以這裡記的是**這一格裡對方的位置**，不是那個人的標籤。同一個人
在另一件事上換一格。

**渠道的五個值：**

| 渠道 | 讀者怎麼接觸到它 | 寫法因此跟著什麼變 |
|---|---|---|
| 即時訊息 | 在訊息流裡讀，會被下一則推走 | 一則只講一件事。沒有標題可以承載脈絡，脈絡要寫進句子 |
| 電子郵件 | 在收件匣裡讀，有主旨欄與收件人欄 | 主旨要能單獨被讀懂，它可能被轉寄給你沒預期的人 |
| 票卡留言 | 循著一張票的脈絡讀，前後有其他留言 | 只寫增量。脈絡由票承載，不重述 |
| 溝通文件 | 主動開啟，可以跳著讀、反覆讀、搜尋 | 結構要撐得住掃描，結論先行 |
| 簡報 | 講的人控制節奏，讀者不能自己倒回去 | 一頁一件事，數字要能在幾秒內讀完 |

報告、提案、會議記錄、規範都落在溝通文件，讀者跟它們的關係是同一種。簡報另立一值，因為讀者
拿不到節奏的控制權。加新渠道值之前先答中間那一欄：讀者接觸它的方式跟既有五個哪一個都不同嗎。
答不出來就是既有某一格。

**意圖不設鍵。** 意圖記的是「我要對方做什麼」，權責記的是「對方是誰」，兩者是同一件事的
兩端，而後者穩定得多。而且同一個對象乘渠道的格子裡，意圖是可變的，設成鍵會讓一格裂成多筆，
那幾筆的規格卻共用。意圖這個概念不刪，它留在表達方式那副骨架裡，在 Expression 這邊降級成「規格」節的填寫依據，
對應那副骨架「由意圖決定句式」那一步。

**呈現方式（表格、條列、圖、散文）同樣不設鍵**，寫在「規格」節裡。四軸是輸入，形狀是輸出，
把輸出拿來當索引鍵會讓同一格出現多筆。

**要加新軸的條件有三個，同時成立才加**：說得出它回答哪一題而且與既有各軸不重疊；有兩筆現存
條目因為缺它而撞在同一格、寫法卻不同；固定其他軸的值它還能獨立變動。第二個條件是唯一的煞車，
沒有它軸會依預想增加，而空格不是待辦，是當初不該切出來的刻度。條件不成立時那個區別寫進
「規格」節，它不會消失，只是不當索引鍵。

引用效率的規則：族譜關係一律走 `upstream:`，近似但不等價的對照走 frontmatter `related:`。有共同
上游的兄弟條目不再互寫增補。兄弟經由上游找到，`recall` 的一跳鄰居沿 upstream 展開。新增成員時
不用回頭改 N 筆。橫向互補網每加一筆成本是 O(N)，上行結構是 O(1)。

| 層 | 目標：回答什麼問題 | 權責：何時被檢索、拿來做什麼 | 異動條件 | 穩定度期望 |
|---|---|---|---|---|
| `Models/` | 為什麼這樣思考。這個判斷背後不變的理由 | 界定新問題、選判斷框架時 | 信念被實證推翻，或被更上游的信念收斂 | 最穩定。改動要沿連結傳播檢查 |
| `Frameworks/` | 這類事該照什麼步驟、用什麼方法想、注意什麼 | 執行一類任務、展開一類思考時 | 步驟被實踐修正、新教訓追加、被 Models 的信念重整 | 中等。追加常見，重寫少見 |
| `Decisions/` | 踩到這個情境時該怎麼判 | 踩到具體情境時、寫入前 dedupe 時 | 新增最頻繁。被再次印證時追加 `confirmed:` | 單筆穩定，整層持續長大，是升級的原料 |

四個異動方向，各自的判準與辦法：

- 升級，下層到上層。多筆下層條目共享同一結構時，抽出上層條目，成員加 `upstream:` 連上。工具
  可算出的指標是 `stack shape` 的群距離過近。人工判準是「這幾筆的差別只剩情境，判斷邏輯是同
  一條」。收斂抽出的上游，`origin` 與 `recorded` 沿用組內最早的母條目，正文列出實例成員。拆分
  沿用單一母條目的規則見 §frontmatter，收斂是它的多母版本。
- 降級，上層到下層。條目通不過本層判準就移到符合判準的那一層。Models 條目拿掉情境就判斷不了
  新問題，Frameworks 條目其實綁單一產物，都是。移層等於 `git mv` 加改 `type:` 加更新 `MEMORY.md`
  與所有指向它的連結。`origin` 與 `recorded` 不動，整理不是新內容進棧。
- 傳播，上層校正下層。上層條目改精準後，沿 `upstream:` 反向與 `downstream:` 逐筆檢查下層是否
  還一致。下游不會自動跟上，而且失效不產生訊號。
- 淘汰，出場。工具可算出的指標是 `stack usage` 的直接命中與鄰居觸及皆為零。`recall` 每次查詢
  自動記使用 log，本機、不進版控。兩欄分開記：直接命中是向量檢索命中，鄰居觸及是命中條目手接
  的連結把它帶進視野。只看直接命中會誤殺 Model 層。實測過一筆 Model 直接命中 0 次，卻在同期
  真的影響了判斷並被追加 `confirmed`，它一直是被命中條目的連結帶進視野的。補上鄰居欄後，零命中
  的筆數明顯下降。零要成立的前提是 log 覆蓋區間不小於觀察窗，預設 90 天。log 起點之前的缺席
  不是證據：訊號沒出現不等於狀態已改變。人工判準與入棧門檻對稱：說不出未來哪個檢索情境會需要
  它，就出場。出場方式優先合併進上游或相鄰條目，判斷保留、獨立檔消失。其次刪檔加清 `MEMORY.md`
  行。git history 就是回收站，不設 archive 目錄。有任何命中或 `confirmed:` 的不是淘汰對象，那
  已經證明檢索情境存在。兩件事刻意不做。不追蹤「命中後有沒有真的用上」，那個責任在檢索痕跡的
  格式上，見 `tools/context/usage-contract.md` 的留痕。usage 不回饋檢索權重，否則變成自我強化
  的人氣模型。

資料夾維持扁平，不做議題子目錄。查找的實際路徑是 `recall`（常數成本）加 `MEMORY.md` 主題
子節加 `stack tree` 的格狀視圖。實體資料夾只優化沒人走的 `ls`，而且強迫單親，跟多對多的
`upstream:` 打架。單一主題累積超過 30 筆左右時再議。

整理辦法：升降級與淘汰不靠寫入當下的自覺，靠週期性覆盤。程序固化在
[skills/decision-stack-curation](skills/decision-stack-curation/SKILL.md)，屬承載層。可算的指標
來自 `stack shape`、`stack eval`、`stack usage`。觸發是每月覆盤，或單層條目明顯累積時。

### 分層與來源是兩個互不決定的分類維度

`origin` 回答「這條怎麼進來的」，分層回答「這條是什麼」。兩者互不決定：一個心智模型可以由
糾正揭露，一個事件原則也可以從組織的 repo 掃到。不用 origin 推斷該放哪一層。

## 來源標記

正本在 [`tools/context/source-marking.md`](tools/context/source-marking.md)。`origin` 的七個值與
各自的門檻、最容易混的兩組分界、各來源帶進寫入程序的四個參數都在那裡。

它住在 `tools/context/` 而不是這裡，因為寫入程序 step 1 的門檻判定要靠它，而 README 不在任何
harness 的保證載入通道裡。判準只放在 repo 裡等於 step 1 在執行時沒有依據。

## 寫入程序

正本在 [`tools/context/write-procedure.md`](tools/context/write-procedure.md)。七步：判定門檻、
去識別化、定層加基準閘門、dedupe、寫入、索引加傳播、分支加 commit 加 PR。含 §兩道閘門的規則句。
它同時是各 harness 保證載入通道 import 的片段，所以判準只有那一份。這裡不重述。重述就會變成
第二個各自演化的版本。

所有來源共用那一份程序。來源之間只帶四個參數進來：`origin` 值、commit 前綴、要不要寫 mem0、
能不能 push。

跳過不會有訊號的是三步：step 3 基準閘門、step 4 dedupe、step 7 分支加 PR。

## 兩道閘門

正本在 [`tools/context/write-procedure.md`](tools/context/write-procedure.md) §兩道閘門。兩句話版。
trunk 閘門：寫入自動但寫在分支上，`main` 的定義是「已審」不是「最新」。閘門架在內容開始生效的
邊界，不在離開本機的邊界。`stack recall` 讀工作目錄，原則從寫下那一刻就在影響判斷。
mem0 鏡射閘門：只有 `origin: judgment_case` 寫 mem0，`MEMORY.md` 這一側才是完整集合。

沒有任何自動化路徑會合併 PR。未合併的分支數持續變大時那是待辦不是狀態。報告要明說累積幾個
與最舊一個的日期。

閘門啟用之前就在 `main` 上的 commit 當成一次性積欠，不追溯重整。在正本內容的歷史上動刀，風險
高於它解決的問題。規則只約束啟用之後的寫入。

## 目錄

- `Models/`：核心信念。一句信念句加適用界線。定義見 §內容分層 表。
- `Frameworks/`：思考骨架。格式、結構、步驟、並列判準。
- `Decisions/`：事件原則。信念或骨架在單一事件上的具體結果。
- `Expressions/`：一格產出的用詞、語氣與內容取捨，加上可複製的骨架。檢索鍵是對象與渠道兩個維度。
- `MEMORY.md`：索引，一行一筆。
- `GLOSSARY.md`：詞彙表。外部具名術語的定義與原始定義出處。條目照用縮寫，內文首次出現時連到
  錨點。本表列回引用它的檔案。雙向一致性由 `stack lint` 檢查。
- `tools/`：承載層。`stack` 是檢索與檢查的 CLI。`regression.tsv` 是召回率語料。
- `tools/context/`：承載層。保證載入片段（使用契約、寫入程序、來源標記）與各 harness 的接線清單。
- `tools/local/`：本機區，不進版控。驗證日期、接線筆記、事故證據、具名清單。範本在 `tools/local.example/`。
- `skills/`：承載層。維護這份內容的程序，例如 curation 覆盤。每份開頭寫明使用時機。
- `templates/`：各層的 frontmatter 與分節骨架。新增條目時複製一份改，不從空白寫。
- `examples/`：一組三筆的完整推導鏈，示範分節與 `upstream:` 的形狀。照著看，不搬進來。

## 檢索（承載層）

```bash
stack recall "<一段話>"   # 寫入前的 dedupe、回答前的檢索。top-k 加一跳連結鄰居
                          # 同時掃未合併分支，命中標〔未審〕。--no-branches 可關
                          # --stale-ok：唯讀用舊索引不重嵌，計畫模式或趕時間用
stack tree                # 依 upstream 印格狀結構。瀏覽視角，查找走 recall
stack shape               # 全量群聚：該收斂的群、內在凝聚度最低的拆分候選
stack lint                # 索引↔檔案、連結、frontmatter、upstream、詞彙表、索引過期
                          # --strict：契約 v2 的三項從提醒升為缺陷。--shippable：只掃可升級區
stack eval                # 召回率回歸。改嵌入、換模型、大批拆分之後一定要跑。--save-baseline 存基線
stack usage               # 使用記錄報表：命中統計、log 覆蓋區間、淘汰候選
stack doctor              # 接線體檢，唯讀
stack install --yes       # 接線、建 shim、設 hooksPath
```

工具版本在 `tools/VERSION`。`stack recall` 的輸出格式、frontmatter 欄位、資料夾名視為公開
介面，破壞性變更要提 major。`stack doctor` 會印目前版本。

`stack` 是 `stack install` 建在 `~/.local/bin` 的 symlink，指向 `tools/stack`。任何目錄都叫得動。
契約片段裡寫的就是這個裸名。絕對路徑是每台機器都不一樣的值，不放進要被各 harness 直接載入的
文字裡。

換機器的安裝與各 harness 接線見 [`tools/context/README.md`](tools/context/README.md)。向量索引是
純衍生物，不進版控。Ollama 不在時 `recall` 明確失敗並提示改讀 `MEMORY.md`。功能降級，但會報錯。

為什麼要有這一層：把整份讀進 context 不留痕跡。跳過十次跟做到十次看起來一樣。三種做法的成本
差一到兩個數量級：全部讀進 context，大於只讀 `MEMORY.md`，大於 `recall` 一次。而且成長方向相反：
前兩者隨條目數線性成長，`recall` 是常數，固定 top-k。差距只會愈拉愈開。想知道自己這一份的實際
倍數，量一次就有。省下來的不只是 token，是覆蓋率保證從自律換成了程式迴圈。

檢索品質不會因此變差，這件事要有證據。`stack eval` 是唯一的證據，改任何東西之後都要跑。基線
用 `--save-baseline` 存檔，不抄進文件。抄進來的數字會跟實際值分岐。判準是改動前後的比較，不是
絕對值。掉出 top-3 的那幾筆若差距在平手帶寬內，屬排序雜訊，記錄不修。回歸語料本身也要維護：
條目拆分後內容換了擁有者，過期的期望照語料內註解修掉。不把新條目硬塞進期望去湊分。

## 使用契約（什麼時候檢索、怎麼造查詢句）

正本在 [`tools/context/usage-contract.md`](tools/context/usage-contract.md)。兩個觸發時機、查詢句
造法、留痕行、降級行為都在那裡。

它放在 `tools/context/` 而不是這裡，因為契約只寫在 README 時，實際生效的會是某個 harness 設定檔
裡的手抄本。換機器或換工具就丟了，而兩份必然分岐。片段是正本，各 harness 的保證載入通道只放
指標，清單見 [`tools/context/MANIFEST.tsv`](tools/context/MANIFEST.tsv)。

